# Runtime
polymarket-us==0.1.2   # Polymarket US SDK (REST + order signing)
cryptography>=41.0.0   # RSA-PSS request signing for Kalshi
pynacl>=1.5.0          # Ed25519 signing for the raw Polymarket US websocket transport
websockets>=12.0.0     # both venues' live feeds
aiohttp>=3.9.0         # async HTTP for the Kalshi client
requests>=2.31.0       # synchronous notification delivery (off the event loop)
httpx>=0.27.0          # error types raised by the Polymarket US SDK's transport
python-dotenv>=1.0.0
discord-webhook>=1.3.1

# Test
pytest>=9.0.0
pytest-asyncio>=0.23.0
